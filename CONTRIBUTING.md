# Contributing

PRs welcome. Please:
- Keep changes backward compatible where possible
- Add a short note to README for new flags/features
- Avoid committing secrets (`config.json` is gitignored)
